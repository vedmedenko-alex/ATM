<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMapper">

    <select id="findById" parameterType="int" resultType="User">
        SELECT * FROM Users WHERE user_id = #{id}
    </select>

    <select id="findByCardAndPin" parameterType="map" resultType="User">
        SELECT * FROM Users WHERE card_number = #{cardNumber} AND pin = #{pin}
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO Users (name, card_number, pin, phone)
        VALUES (#{name}, #{cardNumber}, #{pin}, #{phone})
    </insert>

    <update id="update" parameterType="User">
        UPDATE Users
        SET name=#{name}, card_number=#{cardNumber}, pin=#{pin}, phone=#{phone}
        WHERE user_id=#{userId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM Users WHERE user_id_
